spring:
  application:
    name: stock-anomaly-detector
  
  kafka:
    bootstrap-servers: pkc-619z3.us-east1.gcp.confluent.cloud:9092
    
    properties:
      security.protocol: SASL_SSL
      sasl.mechanism: PLAIN
      sasl.jaas.config: 'org.apache.kafka.common.security.plain.PlainLoginModule required username="XLF7JD6BD7WWBXBS" password="cfltIAN2jnL/LKUrKz13EvAm8n0BKbcrstwrzTFMTAxMHy7rhfyvVcRYAbfubNrg";'

    streams:
      application-id: stock-processing-streams-v1
      replication-factor: 3
      properties:
        default.key.serde: org.apache.kafka.common.serialization.Serdes$StringSerde
        default.value.serde: org.springframework.kafka.support.serializer.JsonSerde

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

    consumer:
      group-id: stock-consumers
      auto-offset-reset: earliest

      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer

      properties:
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: "*"
        # IMPORTANT: Removed default.type !!

app:
  topic:
    input-prices: stock-prices-input
    output-ticker: stock-ticker-output
    output-anomaly: stock-anomaly-alerts
